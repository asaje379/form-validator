{
  "name": "@asaje/form-validator",
  "version": "1.0.8",
  "main": "dist/index.js",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.18.1",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "build": "tsc && esbuild src/*.ts --outdir=dist --minify"
  },
  "files": [
    "dist"
  ],
  "types": "./dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/asaje379/form-validator.git"
  }
}
